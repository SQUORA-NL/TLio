{
  "SlugCache": {
    "MaxBodySizeBytes": 10485760
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Nuplane": {
    "Sources": {
      "Directory": {
        "Path": "/plugins",
        "Watch": true,
        "DebounceWindow": "00:00:02"
      }
    },
    "Loading": {
      "ActiveStoreRoot": "/tmp/nuplane-store"
    },
    "Reconciliation": {
      "//": "NuPlane defaults to FailHost: one unloadable package in /plugins takes the whole API down at startup. This host is a plugin sandbox — an unreadable file is an expected input, not a fatal condition — so it starts and reports the package as failed instead.",
      "StartupFailurePolicy": "StartDegraded"
    }
  }
}
