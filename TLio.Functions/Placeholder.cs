// Functions will be added here as part of the TLio implementation.
// Each function should extend FunctionBase<TNode> from TLio.Core.Models.
//
// Examples to implement:
//   - concat(a, b)      — string concatenation
//   - toUpper(s)        — string transform
//   - now()             — current timestamp
//   - typeOf(node)      — returns the type name of the node
//   - count(path)       — count matched nodes
//   - sum(path)         — numeric aggregate
//
// See specs/001-tlio-core-architecture/tasks.md for the full backlog.

namespace TLio.Functions;
